let shown = false;

ModAPI.addEventListener("update", () => {
if (!shown) {
shown = true;

```
    ModAPI.displayToChat({
        msg: "§6🔥 §cFIRE STAFF MOD IS ACTIVE! §6🔥"
    });
}
```

});
